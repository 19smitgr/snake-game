// initialize canvas to draw on and context to draw with
let canvas = document.getElementById("snakeGame");
let ctx = canvas.getContext("2d");

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;